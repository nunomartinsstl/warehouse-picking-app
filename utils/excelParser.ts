// This utility has been removed as Excel imports are no longer required.
export const parseExcel = async (file: File): Promise<any[]> => {
  return [];
};

export const processOrderFile = (data: any[]): any[] => {
  return [];
};

export const processStockFile = (data: any[]): any[] => {
  return [];
};

export const processLayoutFile = (data: any[]): any[] => {
  return [];
};